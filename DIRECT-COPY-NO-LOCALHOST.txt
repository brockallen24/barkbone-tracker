╔═══════════════════════════════════════════════════════════════╗
║   💻 DIRECT COPY - NO LOCALHOST NEEDED (GUARANTEED TO WORK)  ║
╚═══════════════════════════════════════════════════════════════╝

LOCALHOST ISN'T WORKING - USE THIS DIRECT COPY METHOD INSTEAD:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This bypasses all networking and copies the file directly from
WSL to your Windows Downloads folder.


STEP 1: Open Windows PowerShell
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Press Windows Key
2. Type: PowerShell
3. Click "Windows PowerShell" (or "Run as Administrator")


STEP 2: Copy and Paste THIS EXACT COMMAND:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

$filePath = "$env:USERPROFILE\Downloads\BarkboneTracker.html"; wsl cat /home/user/barkbone-tracker/BarkboneTracker-Clean.html | Out-File -FilePath $filePath -Encoding UTF8 -NoNewline; Write-Host "File saved to: $filePath" -ForegroundColor Green


STEP 3: Press Enter
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You should see: "File saved to: C:\Users\[YourName]\Downloads\BarkboneTracker.html"


STEP 4: Verify the Download
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In PowerShell, run:

Get-Item "$env:USERPROFILE\Downloads\BarkboneTracker.html"

You should see the file with a size around 69-70 KB


STEP 5: Open the File in Chrome
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option A - From PowerShell:

Start-Process chrome.exe "$env:USERPROFILE\Downloads\BarkboneTracker.html"


Option B - From File Explorer:

1. Open File Explorer (Windows Key + E)
2. Go to Downloads folder
3. Find: BarkboneTracker.html
4. RIGHT-CLICK → "Open with" → Google Chrome


STEP 6: Check If It Works
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When the file opens in Chrome, you should see:

✅ PURPLE gradient background
✅ "Inventory Tracker" title at the top
✅ "Connected to Cloud" status indicator
✅ Stats cards showing "Total Products: 0"
✅ Buttons: "Add New Product", "Refresh", etc.

YOU SHOULD NOT SEE:
❌ HTML code as plain text
❌ Tags like <html>, <script>, <body> visible
❌ White background with code


IF YOU STILL SEE PLAIN TEXT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The file is being opened in a text editor instead of a browser!

FIX IT:
1. Find BarkboneTracker.html in Downloads
2. RIGHT-CLICK on it
3. Select "Properties"
4. Look for "Opens with:" and click "Change..."
5. Select "Google Chrome" or "Microsoft Edge"
6. Check "Always use this app to open .html files"
7. Click OK, then OK again
8. Now double-click the file to open it


THEN DEPLOY TO NETLIFY:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Once you see the purple background and working tracker:

1. Go to: https://app.netlify.com/drop
2. Drag BarkboneTracker.html from Downloads folder
3. Drop it on the Netlify page
4. Wait 10-15 seconds
5. Click the live URL to test your deployed site!


TROUBLESHOOTING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Problem: "wsl: command not found"
Solution: WSL is not installed. Let me know and I'll provide an
alternative method.

Problem: File downloads but shows as text
Solution: Change the file association to Chrome (see above)

Problem: File won't open
Solution: Try opening Chrome first, then drag the HTML file into
the Chrome window.


THE SINGLE COMMAND TO RUN:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

$filePath = "$env:USERPROFILE\Downloads\BarkboneTracker.html"; wsl cat /home/user/barkbone-tracker/BarkboneTracker-Clean.html | Out-File -FilePath $filePath -Encoding UTF8 -NoNewline; Write-Host "File saved to: $filePath" -ForegroundColor Green

Copy this entire line, paste it into PowerShell, and press Enter!

╚═══════════════════════════════════════════════════════════════╝
